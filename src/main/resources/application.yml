spring:
  application:
    name: media
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enable: false
  datasource:
    url: jdbc:mysql://astfx-media-db.cdokmg248px7.sa-east-1.rds.amazonaws.com:3306/astfx_media_db?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: dba_root_media
    password: nUKXQ3C35F2Kcq4_

server:
  port: 8763

eureka:
  instance:
    preferIpAddress: true
  client:
    service-url:
      defaultZone: http://ec2-52-67-63-144.sa-east-1.compute.amazonaws.com:8761/eureka/
